import { hoursToMilliseconds } from "./hoursToMilliseconds.ts";
import { tokenDateExp } from "./tokenDateExp.ts";

export {
  tokenDateExp,
  hoursToMilliseconds
}

export default {
  tokenDateExp,
  hoursToMilliseconds
}
